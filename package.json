{
  "name": "tetrisnake",
  "version": "1.0.0",
  "description": "A fully 3D snake game in a WebGPU-rendered Tetris well (three.js r185).",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "for f in js/*.js test/*.js test/helpers/*.js; do node --check \"$f\" || exit 1; done",
    "start": "npx serve"
  }
}
